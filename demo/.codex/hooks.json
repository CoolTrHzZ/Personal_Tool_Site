{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "d=\"$(git rev-parse --show-toplevel 2>/dev/null || true)\"; if [ -z \"$d\" ]; then d=\"$PWD\"; while [ ! -d \"$d/.agent\" ] || [ ! -d \"$d/.codex\" ]; do [ \"$d\" = \"/\" ] && break; d=\"${d%/*}\"; [ -n \"$d\" ] || d=\"/\"; done; fi; exec python3 \"$d/.codex/hooks/full_delivery_stop.py\"",
            "timeout": 30,
            "statusMessage": "Running Sol-Luna acceptance gate..."
          }
        ]
      }
    ]
  }
}
