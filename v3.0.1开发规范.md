# Personal_Tool_Site V3.0.1

## Universal Tool Runtime 稳定性修复 + Admin 管理体验重构

---

# 一、版本目标

当前版本已经实现：

- Universal Tool Manifest
    
- HTML / ZIP 工具识别
    
- Static Tool Runtime
    
- Import Wizard
    
- Tool Manifest 管理
    
- 工具生命周期
    
- 标签管理
    
- 多语言 Admin
    
- Tool Marketplace
    
- Toolbox Bridge
    

当前不要继续扩展新的大功能。

本版本优先解决：

```
P0
├── HTML 导入向导第三步卡死
├── 导入异常缺乏错误保护
├── staging 暂存目录暴露在 public
└── 导入流程无法可靠完成

P1
├── 标签管理数据源不一致
├── 标签删除/重命名无法覆盖 Tool Manifest
├── 标签一次性全量渲染
├── Sidebar Footer 跑到整个页面最底部
└── 表单布局使用全局 Grid 导致页面结构混乱

P2
├── 工具表格列错位
├── Wizard 提示状态逻辑
├── Preview 权限
├── Admin 操作反馈
└── 自动化测试缺失
```

本版本完成以后，目标达到：

> HTML 工具从拖入文件到完成导入的整条链路可稳定使用，并且 Admin 能够在大数据量和长页面情况下正常管理。

---

# 二、P0：修复 Import Wizard 第三步无法继续

## 2.1 已确认的根因

文件：

```
admin/admin.js
```

当前第三步：

```
function renderWizardStep3() {
  const permissions = wizard.manifest.permissions
  const grid = el('div', 'perm-grid')

  for (const [key, labelKey, hintKey] of PERMISSION_META) {
    grid.append(
      checkField(
        `perm.${key}`,
        permissions[key],
        i18n.t(labelKey),
        i18n.t(hintKey)
      )
    )
  }

  $('#wizard-body').replaceChildren(grid, sandboxPreview)
}
```

这里创建的是：

```
<div class="perm-grid">
```

但是下一步读取时：

```
function collectWizardStep3() {
  const grid = $('#wizard-body .perm-grid')

  const permissions = {
    ...wizard.manifest.permissions
  }

  for (const [key] of PERMISSION_META) {
    permissions[key] =
      grid.elements[`perm.${key}`].checked
  }
}
```

问题：

```
HTMLDivElement
```

没有：

```
.elements
```

属性。

`.elements` 只有：

```
HTMLFormElement
```

拥有。

因此点击：

```
权限
  ↓
下一步
```

时会发生类似：

```
TypeError:
Cannot read properties of undefined
```

于是 Wizard 看起来：

> 卡死在第三步。

---

# 三、第三步正确修复方式

不要临时改：

```
querySelector()
```

来打补丁。

直接把 Permission Grid 设计成真正的 Form。

修改：

```
function renderWizardStep3() {
  const permissions = {
    ...wizard.manifest.permissions
  }

  const form = el('form', 'perm-grid')

  form.addEventListener('submit', event => {
    event.preventDefault()
  })

  for (const [key, labelKey, hintKey] of PERMISSION_META) {
    form.append(
      checkField(
        `perm.${key}`,
        permissions[key],
        i18n.t(labelKey),
        i18n.t(hintKey)
      )
    )
  }

  const sandboxPreview =
    el(
      'p',
      'wizard-sandbox',
      `${i18n.t('wizard.sandbox')} ${buildSandbox(permissions)}`
    )

  form.addEventListener('change', () => {
    const nextPermissions = {}

    for (const [key] of PERMISSION_META) {
      const element =
        form.elements.namedItem(`perm.${key}`)

      nextPermissions[key] =
        element instanceof HTMLInputElement
          ? element.checked
          : false
    }

    sandboxPreview.textContent =
      `${i18n.t('wizard.sandbox')} ${buildSandbox(nextPermissions)}`
  })

  $('#wizard-body').replaceChildren(
    form,
    sandboxPreview
  )
}
```

---

## collectWizardStep3

改成：

```
function collectWizardStep3() {
  const form =
    $('#wizard-body .perm-grid')

  if (!(form instanceof HTMLFormElement)) {
    wizardStatus(
      '权限表单初始化失败，请重新打开导入向导'
    )
    return false
  }

  const permissions = {
    ...wizard.manifest.permissions
  }

  for (const [key] of PERMISSION_META) {
    const element =
      form.elements.namedItem(`perm.${key}`)

    if (!(element instanceof HTMLInputElement)) {
      wizardStatus(
        `权限字段缺失: ${key}`
      )
      return false
    }

    permissions[key] = element.checked
  }

  wizard.manifest = {
    ...wizard.manifest,
    permissions
  }

  return true
}
```

---

# 四、Wizard 必须增加顶层错误保护

当前：

```
$('#wizard-next')
  .addEventListener('click', wizardNext)
```

如果：

```
collectWizardStepX()
renderWizard()
DOM 操作
```

发生 JS Error，

页面不会给用户正确提示。

结果就是：

> 按钮按了没反应。

需要统一改：

```
$('#wizard-next')
  .addEventListener('click', async () => {
    try {
      await wizardNext()
    } catch (error) {
      console.error(
        '[ImportWizard]',
        error
      )

      wizard.busy = false

      wizardStatus(
        error instanceof Error
          ? error.message
          : '导入向导发生未知错误'
      )

      renderWizard()
    }
  })
```

`wizardPrev()` 同样不要允许异常把整个向导打死。

---

# 五、Wizard 状态需要正式建模

当前：

```
const wizard = {
  step: 1,
  file: null,
  analysis: null,
  manifest: null,
  overwrite: false,
  busy: false
}
```

升级：

```
const wizard = {
  step: 1,

  file: null,
  analysis: null,
  manifest: null,

  overwrite: false,

  busy: false,

  error: null,
  warning: null,
  notice: null,

  previewReady: false
}
```

以后：

```
错误
warning
兼容性风险
导入状态
```

不能全部塞：

```
#wizard-status
```

一个字符串。

---

# 六、当前 sameOrigin Warning 逻辑也有问题

当前 Step 3：

```
if (permissions.sameOrigin) {
  wizardStatus(
    i18n.t('wizard.sameOriginWarning')
  )
}
```

但是随后：

```
if (wizard.step === 3)
  wizardStatus('')

wizard.step += 1

wizardStatus('')
```

马上又清空。

所以警告实际上几乎不会被用户看到。

改为：

```
wizard.warning =
  permissions.sameOrigin
    ? i18n.t('wizard.sameOriginWarning')
    : null
```

然后 Step 4：

```
兼容性检查

⚠ 已启用同源访问
该工具可能访问当前站点 Cookie / localStorage
仅对可信工具开启
```

必须保留到用户确认。

---

# 七、P0：staging 目录必须立即移出 public

这是这次检查发现的另一个重要问题。

当前：

```
const toolsDir =
  join(publicDir, 'tools')

const stagingDir =
  join(toolsDir, '.staging')
```

最终：

```
public/
└── tools/
    └── .staging/
        └── <token>/
            └── package/
                └── index.html
```

而当前代码包中实际上已经存在：

```
public/tools/.staging/.../package/index.html
```

这说明失败的导入确实已经留下暂存文件。

这是错误设计。

原因：

```
public/
```

是正式静态资源目录。

暂存工具不应该存在这里。

---

# 八、staging 正确架构

改：

```
Personal_Tool_Site/

├── public/
│   └── tools/
│       └── 正式工具
│
└── .tool-staging/
    └── 临时导入文件
```

或者：

```
OS tmpdir
```

建议：

```
const stagingDir =
  resolve(root, '.tool-staging')
```

并加入：

```
.tool-staging/
```

---

# 九、Preview 不再直接通过 `/tools/.staging`

新增专用 Preview API：

```
/__tool_preview/
```

例如：

```
http://127.0.0.1:4174/
__tool_preview/
abc12345/
index.html
```

server：

```
if (
  url.pathname.startsWith(
    '/__tool_preview/'
  )
) {
  return servePreviewAsset(
    req.url,
    res
  )
}
```

路径必须限制：

```
stagingDir
```

内部。

禁止：

```
../
```

穿越。

---

# 十、导入完成后的流程

正确链路：

```
上传

↓

.tool-staging/token/

↓

分析

↓

Wizard

↓

预览

↓

确认导入

↓

校验 manifest

↓

写 temporary target

↓

atomic rename

↓

public/tools/{id}

↓

rebuild tools-manifests.json

↓

删除 staging

↓

reload Admin
```

---

# 十一、关闭 Wizard 也必须清理 staging

当前关闭 Wizard：

```
const closeWizard = () => {
  detachWizardPreview()
  $('#wizard').hidden = true
}
```

但是没有清理 server staging。

建议增加：

```
DELETE
/api/tools/staging/{token}
```

close：

```
async function closeWizard() {
  detachWizardPreview()

  if (wizard.analysis?.token) {
    await request(
      `tools/staging/${wizard.analysis.token}`,
      {
        method: 'DELETE'
      }
    ).catch(() => {})
  }

  $('#wizard').hidden = true
}
```

同时 server 保留：

```
TTL cleanup
```

作为第二保险。

---

# 十二、P1：标签管理现在存在真正的数据逻辑 Bug

目前：

```
allTags()
```

统计：

```
navigation tags
+
tool tags
```

也就是说 Admin 页面显示的是：

> 网站标签 + Tool Manifest 标签。

这是合理的。

但是：

```
rewriteTags()
```

当前只修改：

```
navigation
```

没有修改：

```
Tool Manifest
```

所以会发生：

```
标签 "cs2"

来源：
工具 cs2-color-text

↓

点击删除

↓

navigation 没有这个标签

↓

reload

↓

tools 重新加载

↓

cs2 又出现
```

或者：

```
重命名
assistant → ai-assistant

↓

网站改了

↓

工具没改

↓

最终出现新旧两个标签
```

这是确定需要修的逻辑问题。

---

# 十三、不要继续由浏览器循环改标签

现在：

```
for (const item of state.navigation) {
  await request(...)
}
```

这种做法不适合标签管理。

下一版：

> 标签应该成为 Server-side Management API。

---

# 十四、新增 Tag API

建议：

```
GET
/api/tags


POST
/api/tags/rename


DELETE
/api/tags/:name
```

---

## GET /api/tags

返回：

```
{
  "items": [
    {
      "name": "cs2",
      "total": 2,
      "navigationCount": 0,
      "toolCount": 2,
      "sources": [
        {
          "type": "tool",
          "id": "cs2-color-text",
          "name": "CS2 彩色字体"
        }
      ]
    }
  ]
}
```

---

# 十五、Tag Source of Truth

Tag API 必须扫描：

```
src/data/navigation.json

+

src/tools/manifests/core.json

+

public/tools/*/manifest.json
```

因为当前：

```
内置 React Tool
```

的 Source of Truth 是：

```
src/tools/manifests/core.json
```

而 Static Tool 的 Source of Truth 是：

```
public/tools/{id}/manifest.json
```

不要修改：

```
public/tools-manifests.json
```

因为它只是索引生成文件。

---

# 十六、重命名标签

例如：

```
POST /api/tags/rename
```

```
{
  "from": "cs2",
  "to": "counter-strike"
}
```

Server：

```
读取 navigation

读取 core manifests

读取 static manifests

↓

检查 from usages

↓

替换

↓

数组去重

↓

写 navigation

↓

写 core manifest

↓

写 static manifest

↓

rebuildToolIndex()

↓

返回影响数量
```

返回：

```
{
  "ok": true,
  "affected": 3,

  "navigation": 1,
  "tools": 2
}
```

---

# 十七、删除标签

不能：

```
点击
↓
直接删
```

应该：

```
删除标签 "assistant"？

该标签当前被：

2 个工具
1 个网站

使用。

删除后会从这些资源的 tags 中移除。
```

用户确认：

才执行。

---

# 十八、标签页当前的另一个问题：全量渲染

当前：

```
renderTags() {
  allTags().map(...)
}
```

意味着：

```
10 tags
100 tags
500 tags
```

全部创建 DOM。

所以现在截图看到：

> 一个标签占整整一行，一直向下无限延伸。

不是标签数据本身错误。

而是：

> UI 渲染策略错误。

---

# 十九、标签页重新设计

不要继续：

```
标签管理

[原标签] [新标签] [重命名]

标签1                             删除
标签2                             删除
标签3                             删除
...
```

升级：

```
标签管理

42 个标签
17 个网站标签
31 个工具标签

┌─────────────────────────────────────┐
│ 🔍 搜索标签...     来源：全部 ▼     │
│                   排序：使用次数 ▼ │
└─────────────────────────────────────┘


标签               使用     来源        操作
──────────────────────────────────────────
assistant           12       网站+工具     查看
ai                   8       工具          查看
cs2                  3       工具          查看
docker               2       网站          查看


           < 1  2  3 >
```

---

# 二十、标签分页

默认：

```
pageSize = 20
```

支持：

```
20
50
100
```

不要一次全部显示。

---

# 二十一、标签详情 Drawer

点击：

```
assistant
```

右侧打开：

```
标签详情


assistant

使用次数 12


使用位置

工具
├ AI Prompt
├ JSON Helper
└ xxx


网址
├ ChatGPT
└ Claude


[重命名]

[删除标签]
```

这样：

> 删除/重命名之前知道影响谁。

比现在安全得多。

---

# 二十二、标签重命名表单不要永久占满一整行

当前：

```
<form id="tag-form">
  原标签
  新标签
  重命名
</form>
```

一直展示。

不推荐。

改：

```
点击某一个标签

↓

标签详情 Drawer

↓

重命名
```

或者：

```
选择标签

↓

[重命名]
```

再弹 Modal。

主标签页顶部不需要长期放重命名表单。

---

# 二十三、P1：Sidebar Footer 跑到整个页面最底部的原因

当前：

```
.admin-shell {
  display:grid;
  grid-template-columns:
    248px minmax(0,1fr);

  min-height:100vh;
}
```

sidebar：

```
.sidebar {
  display:flex;
  flex-direction:column;
}
```

footer：

```
.sidebar-footer {
  margin-top:auto;
}
```

问题：

Grid 两列默认会按照最高内容拉伸。

如果：

```
标签页面高度 = 3000px
```

Sidebar 也被拉到：

```
3000px
```

于是：

```
margin-top:auto
```

把 Footer 放到：

> 整个 3000px 页面最下面。

所以你只有滚到网页最底部才能看到：

```
打开站点
CMS · 本地 · 就绪
```

---

# 二十四、Sidebar 正确布局

桌面：

```
.admin-shell {
  display:grid;
  grid-template-columns:
    248px minmax(0,1fr);

  min-height:100vh;

  align-items:start;
}


.sidebar {
  position:sticky;

  top:0;

  height:100dvh;

  display:flex;

  flex-direction:column;

  overflow:hidden;
}


.sidebar nav {
  flex:1;

  min-height:0;

  overflow-y:auto;
}


.sidebar-footer {
  flex:none;

  margin-top:16px;

  padding-top:16px;
}
```

效果：

```
┌────────Sidebar────────┐
│ Logo                  │
│                       │
│ 仪表盘                 │
│ 网站导航               │
│ 工具管理               │
│ ...                   │
│                       │
│        scroll area    │
│                       │
├───────────────────────┤
│ 中文                  │
│ 打开站点 ↗            │
│ ● CMS 本地就绪        │
└───────────────────────┘
```

无论右侧：

```
500px
5000px
```

Sidebar 状态始终在视口底部。

---

# 二十五、移动端必须恢复正常文档流

```
@media (max-width:760px) {

  .sidebar {
    position:static;

    width:100%;

    height:auto;

    overflow:visible;
  }

  .sidebar nav {
    overflow:visible;
  }

}
```

---

# 二十六、P1：当前 Admin 表单 CSS 设计过于宽泛

当前有：

```
.panel form {
  display:grid;

  grid-template-columns:
    repeat(
      auto-fit,
      minmax(160px,1fr)
    );
}
```

这条规则会作用于：

```
网址表单
分类表单
标签表单
系统设置
其它 Panel Form
```

所以不同功能的表单被：

> 同一种 auto-fit Grid 强行布局。

这是很多“挤”“怪”“全量平铺”视觉问题的根源。

---

# 二十七、删除 `.panel form` 通用布局

改成：

```
.panel form {
  margin-bottom:18px;
}
```

不同表单自己控制布局。

---

# 二十八、网站表单

```
<form
  id="nav-form"
  class="form-grid website-form"
>
```

CSS：

```
.website-form {
  display:grid;

  grid-template-columns:
    repeat(12,minmax(0,1fr));

  gap:12px;
}
```

例如：

```
名称          3列
URL           5列
分类          2列
启用          2列

描述          6列
Tags          4列
Order         2列
```

---

# 二十九、分类表单

```
.category-form {
  display:grid;

  grid-template-columns:
    2fr 3fr 2fr 1fr auto;

  gap:12px;
}
```

---

# 三十、系统设置不要一排 6 个 Input

改成：

```
基本信息

站点名称
[.............................]

页面标题
[.............................]


SEO

描述
[.............................]


链接

GitHub
[.............................]


外观

Logo
[........]

Footer
[.............................]
```

Desktop：

```
2 columns
```

而不是：

```
6 inputs in one row
```

---

# 三十一、P1：工具管理 Table 当前存在列错位

Admin HTML：

```
ID
名称
Version
Runtime
Format
状态
操作
```

共：

```
7 columns
```

但是 `renderTools()` 当前实际生成：

```
[
  tool.id,
  tool.name,
  version,
  `${runtime} / ${format}`
]
```

再追加：

```
status
actions
```

总共只有：

```
6 td
```

也就是说：

```
Runtime + Format
```

被合到一个 Cell，

但 Header 仍是两个。

修：

```
for (const value of [
  tool.id,

  `${tool.name}${
    tool.runtime === 'react'
      ? ' · core'
      : ''
  }`,

  `v${tool.version}`,

  tool.runtime,

  tool.format || '-'
]) {
  row.append(
    text('td', value)
  )
}
```

然后：

```
status
actions
```

最终刚好 7 列。

---

# 三十二、P1：Preview 权限完善

你现在的：

```
sandbox
```

主要控制：

```
scripts
forms
modals
downloads
popups
same-origin
```

但例如 Legacy HTML：

```
navigator.clipboard.writeText()
```

在 iframe 内还可能受到：

```
Permissions Policy
```

影响。

所以：

```
clipboard = true
```

时：

Admin Preview：

```
frame.setAttribute(
  'allow',
  'clipboard-read; clipboard-write'
)
```

前端 StaticToolPage：

```
allow={
  tool.permissions.clipboard
    ? 'clipboard-read; clipboard-write'
    : undefined
}
```

---

# 三十三、注意：network 权限现在不是真正的安全隔离

当前 Admin 写：

```
外部网络
允许请求外部 API / CDN
```

但 iframe sandbox：

> 并不能因为 network=false 自动阻止 fetch。

所以当前：

```
network
```

更多只是：

> Capability Declaration

而不是：

> Security Enforcement。

不能给用户错误安全暗示。

UI 建议显示：

```
外部网络

兼容性声明

⚠ 当前浏览器 Sandbox 无法彻底阻断网络请求
```

未来如果真的需要网络隔离，再做：

```
CSP
```

策略。

---

# 三十四、权限 UI 应该分组

不要现在 9 个权限全部同级展示。

改：

```
平台能力

☑ 剪贴板
☐ 键值存储
☐ 浏览器通知


浏览器 Sandbox

☐ 原生弹窗
☐ 文件下载
☐ 弹出窗口
☐ 同源访问


外部能力

☐ 外部链接
☐ 外部网络
```

---

# 三十五、危险权限需要单独视觉提示

```
sameOrigin
popups
network
```

展示：

```
⚠ 高风险
```

特别：

```
sameOrigin
```

不能和：

```
clipboard
```

视觉等级一样。

---

# 三十六、Import Wizard 下一阶段重新规划

最终 6 步：

```
1 识别

2 元数据

3 权限

4 兼容性

5 预览

6 确认导入
```

保持不变。

但是实现方式：

```
StepRenderer

+

StepValidator

+

WizardState
```

不要继续：

```
if step === ...
```

散在函数里。

---

# 三十七、定义 Step Registry

例如：

```
const WIZARD_STEPS = {

  1:{
    render:renderDetect,
    validate:validateDetect
  },

  2:{
    render:renderMetadata,
    collect:collectMetadata,
    validate:validateMetadata
  },

  3:{
    render:renderPermissions,
    collect:collectPermissions,
    validate:validatePermissions
  },

  4:{
    render:renderCompatibility
  },

  5:{
    render:renderPreview,
    validate:validatePreview
  },

  6:{
    render:renderImport
  }

}
```

Wizard Next：

```
async function wizardNext(){

  const current =
    WIZARD_STEPS[wizard.step]

  if(current.collect){

    const result =
      await current.collect()

    if(!result)
      return

  }

  if(current.validate){

    const result =
      await current.validate()

    if(!result)
      return

  }

  wizard.step++

  renderWizard()

}
```

这样以后不会再出现：

> Step 3 特殊写法导致整个向导卡死。

---

# 三十八、Import Wizard 需要恢复能力

如果：

```
Step 5 Preview失败
```

不要重新上传。

允许：

```
返回 Step 4
修改权限
重新预览
```

如果：

```
导入失败
```

保留：

```
analysis
manifest
staging token
```

允许：

```
重试导入
```

直到用户关闭 Wizard。

---

# 三十九、Admin 操作状态不要再长期放 Header

当前：

```
已更新
已保存
本地内容管理
```

都写到：

```
#status
```

建议：

Header：

只显示稳定状态：

```
本地内容管理
```

操作反馈：

改 Toast：

```
✓ 网站已保存

✓ 标签已重命名

✓ 工具导入成功

✕ Manifest 校验失败
```

右上角：

3 秒自动消失。

---

# 四十、下一步必须增加自动化测试

现在这个 Bug：

```
div.elements
```

Lint / TypeScript 都很难发现，

因为：

```
admin/admin.js
```

是原生 JS。

所以需要真实 DOM 测试。

---

# 四十一、推荐增加 Vitest + jsdom

增加：

```
tests/

├── admin/
│   ├── import-wizard.test.js
│   └── tags.test.js
│
└── manifest/
    └── tool-manifest.test.js
```

至少测试：

```
Step 1 → Step 2

Step 2 → Step 3

Step 3 → Step 4

Step 4 → Step 5

Step 5 → Step 6
```

---

# 四十二、再增加 Playwright E2E

这一项目已经值得做 E2E。

测试：

```
启动 npm run admin

↓

上传 test-tool.html

↓

识别成功

↓

填写 metadata

↓

权限

↓

兼容性

↓

预览

↓

导入

↓

GET /api/tools

↓

出现 test-tool
```

这个测试一旦存在：

> 以后高级模型再修改 Wizard，很难重新把整个流程弄坏。

---

# 四十三、专门用现有 CS2 HTML 做验收

建议把：

```
community_rainbow_chat_generator_v4_unicode_fix.html
```

作为：

```
fixtures/
tools/
cs2-rainbow.html
```

或者测试 fixture。

验收要求：

```
上传 HTML

↓

自动识别 title

↓

自动生成 id

↓

Clipboard 默认被识别

↓

权限第三步可正常下一步

↓

兼容性显示 Unicode/Clipboard相关信息

↓

Preview 正常运行

↓

彩虹文字生成正常

↓

复制正常

↓

确认导入

↓

生成：

public/tools/{id}/index.html

public/tools/{id}/manifest.json

↓

tools-manifests.json 自动更新

↓

前端出现工具

↓

/#/tools/{id} 可访问
```

---

# 四十四、标签管理验收

至少准备：

```
100 tags
```

确认：

```
默认只显示20个
```

测试：

```
搜索
来源筛选
排序
分页
```

---

标签：

```
assistant
```

同时存在：

```
navigation
core react tool
static tool
```

点击重命名：

```
assistant

↓

ai-assistant
```

必须三个数据源全部修改。

---

# 四十五、Sidebar 验收

标签页制造：

```
4000px
```

高度。

页面滚动：

```
0
1000
3000
```

Sidebar：

```
Logo
Navigation
Footer
```

始终保持完整。

Footer：

```
中文
打开站点
CMS · 本地 · 就绪
```

始终在当前窗口底部。

---

# 四十六、开发阶段

## Phase 1 — P0 Wizard Hotfix

修改：

```
admin/admin.js
```

完成：

```
Step3 form修复
Wizard error boundary
Warning state
Next/Prev保护
```

提交：

```
fix(admin): repair import wizard permission step
```

---

# 四十七、Phase 2 — Staging Security

修改：

```
scripts/admin-server.mjs
.gitignore
```

完成：

```
staging移出public

Preview独立路由

关闭清理

TTL清理

Import完成清理
```

提交：

```
fix(import): isolate tool staging from public assets
```

---

# 四十八、Phase 3 — Tag Domain API

新增：

```
/api/tags
```

实现：

```
TagUsage

TagRename

TagDelete
```

统一：

```
navigation
core manifest
static manifest
```

提交：

```
feat(admin): add unified tag management api
```

---

# 四十九、Phase 4 — Tags UI

完成：

```
Search

Source Filter

Pagination

Usage Drawer

Rename Modal

Delete Impact
```

提交：

```
feat(admin): redesign tag management
```

---

# 五十、Phase 5 — Admin Layout

完成：

```
Sticky Sidebar

Viewport Footer

Form Specific Grid

Settings Sections

Tool Table Column Fix

Toast
```

提交：

```
fix(admin-ui): refine console layout and forms
```

---

# 五十一、Phase 6 — Preview Permission

完成：

```
Clipboard Permissions Policy

Dangerous Permission Badge

Network Capability说明

Sandbox Preview
```

提交：

```
fix(runtime): align preview permissions with static runtime
```

---

# 五十二、Phase 7 — Automated Testing

增加：

```
Vitest

jsdom

Playwright
```

package scripts：

```
{
  "test": "vitest run",
  "test:e2e": "playwright test"
}
```

提交：

```
test(admin): cover tool import and tag lifecycle
```

---

# 五十三、最终 CI

GitHub Actions：

```
npm ci

↓

npm run validate

↓

npm run lint

↓

npm run typecheck

↓

npm test

↓

npm run build
```

E2E 可以单独：

```
pull_request
```

运行。

---

# 五十四、禁止事项

本轮不要：

```
重新写整个 Admin React

重新做 Tool Runtime

重新设计 Manifest Schema v3

增加数据库

增加账户系统

增加在线 CMS

增加复杂后端框架
```

当前架构够用了。

问题主要是：

> 交互状态、数据一致性和管理 UI。

---

# 五十五、最终验收标准

必须全部通过：

```
[ ] HTML导入可以走完整6步

[ ] Step3不会卡死

[ ] Wizard异常有可见错误信息

[ ] sameOrigin危险提示不会消失

[ ] Preview正常

[ ] Clipboard HTML工具正常

[ ] staging不再位于public

[ ] 关闭向导清理staging

[ ] 成功导入清理staging

[ ] 标签管理不再全量无限渲染

[ ] 标签支持搜索

[ ] 标签支持分页

[ ] 标签显示使用来源

[ ] 标签重命名覆盖Navigation

[ ] 标签重命名覆盖Core Manifest

[ ] 标签重命名覆盖Static Manifest

[ ] 标签删除显示影响范围

[ ] Sidebar Footer始终在viewport

[ ] Settings表单不挤成一行

[ ] Tool Table列数正确

[ ] npm run lint通过

[ ] npm run typecheck通过

[ ] npm run validate通过

[ ] npm test通过

[ ] npm run build通过
```

---

# 五十六、最终产品行为

修复完成以后：

```
用户拖入 HTML

          ↓

      识别成功

          ↓

       元数据

          ↓

        权限

          ↓

       兼容性

          ↓

        预览

          ↓

      确认导入

          ↓

    Static Tool 安装

          ↓

      Index重建

          ↓

      前端自动出现
```

整个过程：

```
不修改React代码
不修改Router
不手写Manifest
不手动复制HTML
```

这才是 Universal Tool Runtime 这一版真正应该达到的完成标准。